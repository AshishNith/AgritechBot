# --- Frontend environment variables ---
# Set this to your Render backend URL in Vercel dashboard (no trailing slash)
# Leave empty during local dev (Vite proxy handles it)
VITE_API_URL=https://your-backend.onrender.com
