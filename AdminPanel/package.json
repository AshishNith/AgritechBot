{
  "name": "anaaj-admin-dashboard",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix ..\\Backend run dev\" \"npm run dev --workspace frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm --prefix ..\\Backend run dev",
    "build": "npm --prefix ..\\Backend run build && npm run build --workspace frontend",
    "build:frontend": "npm run build --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

